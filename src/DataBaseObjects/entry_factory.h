#ifndef ENTRY_FACTORY_H
#define ENTRY_FACTORY_H

#include <string>

#include "entry.h"


class Entry_Factory{
    public:
        // template <typename Type>
        // Entry<Type>* create_entry(Type data);
        // Entry<double>* create_entry(double data);
        // Entry<char>* create_entry(char data);
        // Entry<std::string>* create_entry(std::string data);
};

#endif